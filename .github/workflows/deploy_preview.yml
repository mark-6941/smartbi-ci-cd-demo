name: Deploy Preview

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number'
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy Preview
        run: |
          echo "Deploy preview for PR #${{ github.event.inputs.pr_number }}"

      - name: Smoke Test
        run: |
          echo "Run smoke test..."
          # 模擬成功
          exit 0
